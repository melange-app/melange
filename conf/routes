# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

module:testrunner

# Basic Index
GET     /                                       App.Index

# Login Related Actions
GET     /login                                  App.Login
POST    /login                                  App.ProcessLogin
GET     /register                               App.Register
POST    /register                               App.ProcessRegistration
GET     /logout                                 App.Logout

# Account Management
GET     /account                                App.Account
POST    /account                                App.ProcessAccount

# Login User Center
GET     /dashboard                              Dispatch.Dashboard
GET     /profile                                Dispatch.Profile
GET     /all                                    Dispatch.All

# App Controllers
GET     /app/:app                               Dispatch.LoadApp
GET     /app/:app/:action                       Dispatch.LoadApp
GET     /app/:app/:action/m/:message            Dispatch.LoadAppMessage
GET     /app/:app/:action/u/:username           Dispatch.LoadAppUser
POST    /app/:app/:action                       Dispatch.AppPost

# GET     /dashboard                              App.Dashboard

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all
*       /:controller/:action                    :controller.:action
