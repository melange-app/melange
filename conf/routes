# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

module:testrunner

# Basic Index
GET     /                                       App.Index

# Login Related Actions
GET     /login                                  App.Login
POST    /login                                  App.ProcessLogin
GET     /register                               App.Register
POST    /register                               App.ProcessRegistration
GET     /logout                                 App.Logout

# Account Management
GET     /account                                Dispatch.Account
POST    /account                                Dispatch.ProcessAccount

POST    /account/subscriptions/new              Dispatch.AddSubscription
POST    /account/subscriptions/:id/remove       Dispatch.RemoveSubscription

# Login User Center
GET     /dashboard                              Dispatch.Dashboard
GET     /profile                                Dispatch.Profile
GET     /all                                    Dispatch.All

# Application Management
GET     /app                                    Dispatch.Applications
POST    /app/new                                Dispatch.AddApplication

# App Controllers
GET     /app/:app                               Loader.LoadApp
POST    /app/:app/uninstall                     Dispatch.UninstallApplication
GET     /app/:app/:action                       Loader.LoadApp
GET     /app/:app/:action/m/:message            Loader.LoadAppMessage
GET     /app/:app/:action/u/:username           Loader.LoadAppUser
POST    /app/:app/send                          Loader.SendMessage
POST    /app/:app/:action                       Loader.AppPost

# GET     /dashboard                            App.Dashboard

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET		/apps/*filepath                         Static.Serve("applications")
GET     /public/*filepath                       Static.Serve("public")

# Catch all
*       /:controller/:action                    :controller.:action
